import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  headerStyle: {
    position: 'absolute',
    zIndex: 0
  },
  backgroundImage: {
    flex: 1,
    resizeMode: 'cover',
    width: '100%'
  }
})

export default styles
